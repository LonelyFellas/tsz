# 实时摘要风险与证据

2026-09-05，在新增测试前确定；现已全部验证。复用既有保存/冲突测试，不扩展后端。

| 风险                                             | 证据                                                                                             |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------ |
| 条目按变体/成员/译文重复计数；词性小计不等于总计 | readiness 纯函数：多词性、共享 form、双 grammar variant、多译文、自身等级、只展示实际等级/未分级 |
| 类型误判、语言/词性硬编码、未知值假完成          | 纯函数：实际语言映射、只区分发音、通用无子行、目录名称、dirty 状态                               |
| 编辑摘要滞后/回第一步摘要读旧数据                | Layout rerender + Wizard/slot 集成：当前 draft 的文本、数量、排序，basics 传入相同 draft         |
| 词义卡标题与左栏不同                             | 共用首条定义摘要纯函数：中文、英式/美式缺项、空定义                                              |
| 原点击定位、只读禁用、返回入口丢失               | 复用 Layout/Wizard 现有导航测试，增加旧信息卡移除断言                                            |
| UD 共用拼写无法承接 UK/US variant issue          | 真实 Wizard 同时注入两侧拼写 issue；唯一共用输入呈现错误态，并可分别按 UK/US target 聚焦         |
| 例句摘要显示不存在等级的 0 行                    | readiness 纯函数与真实页面：只保留实际等级，未分级仅实际存在时显示，等级修改后同步增删           |
| 长文和窄屏不可读，真实页面被测试污染             | 独立端口/隔离 fixture 页面验证桌面、双列、单列和省略；用户原页不操作                             |
| 其他功能回归                                     | 聚焦测试通过后一次 typecheck、lint、Admin build、仓库 test:cov，不降低门槛                       |

实测证据：源功能聚焦 5 文件 152 项通过；整合后与 UD 修复的测试并集 7 文件 232 项通过。实时输入后摘要文本和脏步骤状态立即变化，切换英美后出现 BrE/AmE，返回第一步仍保留草稿；桌面无溢出，平板两列、手机单列。整合后的 `3001` 真实登录词条正常加载；隔离 fixture 同屏显示七组摘要、英美共用拼写和独立 BrE/AmE 发音。Review 修订后，UD 两侧拼写定位与实际例句等级过滤的受影响 3 文件、145 项测试及 Admin typecheck/lint 已通过，并通过独立定向复核。最终本地 `dev` 的根 typecheck、lint、Admin build 与完整覆盖率门通过，覆盖率门为 171 文件、2870 项测试。
