# 实施与验证记录

日期：2026-09-07。前端主目录 tsz/dev 基线 c6d839e；后端主目录 tsz-rust/dev 基线 66e6920。两仓 main 与对应 dev 的 HEAD 相同，本功能保留为 dev 工作区未提交改动；未推送或部署。

## 功能结果

- 多维释义英文正文、英文例句共用关联模式编辑器；原语法结构保留原工具。
- 共用成分用词级联，支持直接目标及释义级短语成分路径、清除、原位回显。
- 按码点保存独立位置，支持连续和非连续片段、标点边界、改字平移或清除、整体撤销。
- 保留连读、停顿和 voice_profile；中文语言切换有数据移除确认。
- 两处保存、读回、预览及历史发布快照保留关联；关联错误定位到所属正文。
- 后端校验目标发布/词形/词义归属、短语成分身份、自引用、重复/重叠/越界，生成只读文案，注册 text_link 发布引用。

## 自动检查

| 检查                            | 结果与证据                                                                                                 |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| 前端 pnpm typecheck / pnpm lint | 全仓通过；收尾 admin 变更再次完成类型与 Lint 检查                                                          |
| pnpm test:cov                   | 184 个文件、3039 个测试通过，完成根脚本前置检查及完整覆盖率报告；未改覆盖配置                              |
| 收尾定向回归                    | 关联错误定位/提示、共用候选选择器 132 项通过；历史快照 40 项、释义页面原 68 项、切换确认/取消新增 2 项通过 |
| admin 生产构建                  | 最终 Vite 构建通过，VOICE_EDITOR=true，全部 API mock=false，语音预览与上传关闭                             |
| Rust fmt                        | cargo fmt --all -- --check 通过                                                                            |
| Rust Clippy                     | cargo clippy --locked --all-targets --all-features -- -D warnings 通过                                     |
| Rust lib                        | cargo test --locked --lib：305 通过，1 项原有 ignored                                                      |
| 真实数据库 handler              | text_links_persist_both_english_fields_publish_and_clear 通过                                              |
| 契约                            | Rust OpenAPI 已导出；前端 snapshot/runtime schema 同步；契约测试包含于 test:cov                            |
| Git                             | 两仓 diff --check 通过                                                                                     |

覆盖率全量完成后补充的历史预览、关联错误定位和切换确认，用定向组件测试覆盖；不把后续新增用例算入 3039 的全量统计。

handler 验证内容：两处保存/GET/发布，直接关联与经短语成分关联；伪造表单目标、自引用、只读文案、错误成分 ID 被拒绝；省略字段保留，旧客户端改正文被拒绝，显式 [] 清除；发布清除版本后激活旧发布，读取历史快照仍保留关联；被 text_link 引用的短语归档返回 409，并明确返回 text_link 来源。

## 浏览器真实链路

使用 Chromium 1600×1100，未设置 page.route 或 service worker 拦截。

- 前端生产制品在 http://127.0.0.1:53001 预览；代理 http://127.0.0.1:58383/api/v1。
- 后端由当前 Rust 代码的真实 router 启动临时验收进程，启用 V3 能力；使用任务隔离数据库与 Redis，测试身份通过真实会话服务签发，浏览器走真实 refresh/profile。
- 进入词义步骤，在「定义 2 通用内容」及「例句 1 通用英文」分别选择 mother → 已发布候选 → 词形 → 词义。
- 设置定义语速 1.25×，将正文前缀改为 Dear，保存草稿。
- PUT 返回 200，两处 text_links 均指向选中发布目标；定义 source_segments 由 [2,8) 平移到 [5,11)，voice_profile.rate_percent=25。
- 整页刷新重新 GET，点击 mother 回显已有目标。未出现页面运行错误。

实际请求结果：POST auth/refresh、GET profile、GET parts-of-speech/catalog、GET entry、POST component-targets/search、PUT steps/meanings 均为 200。录音上传与真实语音合成不在此次验收范围。

界面证据在本地 `/tmp/tsz-text-links-editor.png`、`/tmp/tsz-text-links-browser-verified.png`；测试词义文案为 fixture 占位内容。

## 环境清理与边界

临时后端 58383、前端预览 53001 已停止；任务专用 Postgres 容器 tsz-text-links-pg-20260907 与 Redis 容器 tsz-text-links-redis-20260907 及匿名卷已删除。临时 Rust 验收入口、浏览器脚本与会话明文文件已删除。常驻旧服务及共享数据库未修改。

当前运行服务不代表这些未提交代码已加载；本次不包含提交、推送、合并、部署或重启常驻服务。

## 用户本地验收环境（2026-09-07 16:20 起，保留运行）

用户要求查看本地效果后，重新启动当前 dev 工作区的独立环境。以下状态替代上一节“已清理”的旧验收环境状态；未接管常驻服务。

| 组件     | 当前来源与地址                                                                                  |
| -------- | ----------------------------------------------------------------------------------------------- |
| admin    | 主目录当前 dev，Vite dev，PID 10243，http://127.0.0.1:53001                                     |
| 后端     | 主目录当前 dev 编译的真实 router，测试配置，PID 10484，http://127.0.0.1:58383                   |
| API 代理 | admin `/api/v1` → `http://127.0.0.1:58383/api/v1`                                               |
| Postgres | 容器 `tsz-text-links-pg-20260907`，127.0.0.1:55439，独立库 `text_links_browser`                 |
| Redis    | 容器 `tsz-text-links-redis-20260907`，127.0.0.1:56389/0                                         |
| 开关     | 编辑器开启；words、part-of-speech、TTS API mock 关闭；短信采用本地固定验证码；语音试听/上传关闭 |

验收入口：http://127.0.0.1:53001/words/01a07af4-b830-7f31-93a8-2f1e6533cd58/v3/wizard/meanings 。Codex 内置浏览器已登录并保留此页面。

示例源词条 `a mother's love`；候选 `mother`（妈妈；母亲）与 `my mother`（我的妈妈）。通过正常手机号/密码/验证码登录，界面操作完成释义语句的直接关联和例句经短语成分关联。真实候选查询、保存、刷新读取均返回 200；刷新读回两处各 1 个 text_link，例句包含 via_phrase；healthz=ok、readyz=ready。

环境日志：`/tmp/tsz-text-links-local-admin.log`、`/tmp/tsz-text-links-local-backend.log`。测试启动入口已移至 `/tmp/tsz-text-links-local-launcher.rs`，不混入产品改动；测试账号说明在 `/tmp/tsz-text-links-local-access.md`。本轮未改功能代码，未重复全量质量门。

结束验收后，只处理上述已核实的本任务 PID/容器；删除容器数据前保留用户需要的验收记录。不要按默认端口停止其他常驻服务。

## 短语级联交互修正（2026-09-07）

前置条件：候选搜索返回同一短语的英美命中，并携带相同释义级成分。旧界面在级联下方重复列出两组「my mother · 成分用词」，点击成分又要求选一次词条；与用户确认的四级级联不符。

失败基线：补充预期交互测试后，旧实现 3 项失败、1 项通过，日志 `/tmp/tsz-phrase-cascade-before.log`。失败分别覆盖同面板成分展开、短语本身路径、成分查询失败重试。

修复：仅调整前端共享级联与关联入口的回填参数。按来源身份合并重复候选，短语的第二列直接包含成分词，选到词义才提交；保留「短语本身」入口。已有关联恢复完整四级，成分请求失败可重试，不清除原值。

定向测试 40 项通过（新增关联选择器 5 项、原成分用词 35 项），包含同名但不同来源成分不得误合并；admin 类型与 Lint 检查通过。真实本地页面两处选定短语成分后保存返回 200，响应均包含 via_phrase；刷新后菜单自动恢复四列且词义勾选正确。后端与数据契约无需改动。

修正后的完整 `pnpm test:cov` 通过：184 个测试文件、3047 个测试，覆盖率门通过（日志 `/tmp/tsz-phrase-cascade-coverage.log`）。两仓仍在主目录 dev，本轮后端无新增修改，未提交或推送。

## 拆分关联单词与关联短语（2026-09-07，最终交互）

用户进一步明确两种流程。本节替代上一轮的单一「关联」按钮、Ctrl / ⌘ 多选和「短语本身」交互。

- 两处英文编辑器显示「关联单词」「关联短语」。单词模式仅选择一个源词并查询单词；短语模式普通点击至少两个源词，允许不连续，再点「选择关联短语」。选词阶段不发起候选查询。
- 请求沿用已存在的 kind 参数，返回候选也按类型过滤。短语继续四级级联，不提供「短语本身」，未配置可用成分的短语不可选。原成分用词入口保留原行为。
- 切换模式清空临时选择；再次点击取消一个源词；禁止跨段选择。回显须匹配完整来源区间，选中局部不会自动扩大范围。

先补充测试，旧实现 7 项失败、3 项通过，日志 `/tmp/tsz-split-association-before.log`。修复后编辑器、关联选择器及原成分用词的定向测试 45 项通过，日志 `/tmp/tsz-split-association-after.log`。voice-editor 与 admin 类型检查、Lint、改动文件 Prettier 检查通过。

本轮完整 `pnpm test:cov` 通过：184 个文件、3051 个测试，197.52 秒，原有覆盖率门通过，未修改测试配置或门槛。日志 `/tmp/tsz-split-association-coverage.log`。

真实本地浏览器验证（沿用上述保留运行的隔离环境）：

1. 释义点击「关联单词」及 mother，仅出现 mother 单词候选，请求 kind=word；经过词形、词义完成选择。
2. 例句使用 `My dear mother loved to dress me up.`，普通点击 My 后继续按钮不可用，再点击 mother 后可用。点击「选择关联短语」之前候选请求数为 0；点击后请求 q=`My mother`、kind=phrase，仅出现 my mother 短语。
3. 在同一面板选择 my mother → mother → 原形 mother → 妈妈；母亲。保存 PUT 返回 200。
4. 整页刷新 GET 返回 200：释义关联保存 mother 的 [3,9)；例句保存 My 的 [0,2) 与 mother 的 [8,14)，包含 via_phrase，中间 dear 不属于关联。
5. 重新打开单词选择恢复三级；短语重新选齐两个源词并继续后恢复四级及选中词义。两种按钮及非连续高亮已通过截图检查。

本轮仅修改前端交互，后端及 API schema 无新增改动。两仓仍在主目录 dev，diff --check 通过；未提交或推送。本地服务继续保留，供用户验收。

## 关联 Popover 与禁止重复关联（2026-09-07）

用户确认：选词提示和「选择关联短语」操作放入关联按钮的 Popover；已关联词不得再次参与任何关联，必须先解除。本节替代前一轮的正文下方操作区与可直接改选已有目标的行为。

实现复用工具栏 Popover 的上方定位与 stayOpen 行为。选词期间保留面板，进入候选时收起工具面板。点击已关联词按 ID 查看完整原关联，不能加入新选词；仅提供摘要和清除操作，不发起候选查询。清除按关联 ID 解除整组，其他关联与相同拼写的其他位置保持不变。提交新关联时再次检查重叠，取消原先的覆盖逻辑。原成分用词的替换交互不变。

失败基线：旧实现 4 项失败、8 项通过，覆盖 Popover 容纳操作区、单词/短语互斥、已有目标只查看清除；日志 `/tmp/tsz-association-popover-lock-before.log`。修复后 3 个定向测试文件、47 项通过，包含原成分用词 35 项回归；日志 `/tmp/tsz-association-popover-lock-after.log`。组件测试关闭 antd 动画，验证弹层内容、展开状态和源词点击后仍可继续操作；不以 jsdom 的 CSS 动画状态替代业务状态。

voice-editor 与 admin 的类型检查、Lint，以及改动文件的 Prettier 检查通过；两仓 diff --check 通过。后端与 API schema 无新增改动，两仓仍在 dev。

完整 `pnpm test:cov` 通过：184 个文件、3053 个测试，183.63 秒，原有覆盖率门通过；日志 `/tmp/tsz-association-popover-lock-coverage.log`。未修改覆盖率配置或门槛。

真实浏览器验证：

- 点击关联短语，提示和继续按钮显示在工具栏上方 Popover，原编辑器下方操作区已消失。
- 例句已有 My … mother 短语关联，切换关联单词再点 mother，仅显示原组及清除入口；候选请求为 0。
- 清除原组后可再次选择 My 与 mother，Popover 中显示已选两个词；继续四级级联并保存，PUT 返回 200，两处仍各只有一条关联，例句区间仍为 [0,2) 和 [8,14)。
- 释义已有 mother 单词关联，在短语模式先选 My 再点 mother，后者仅打开原关联；回到选词面板仍只有 My，继续按钮不可用。
- 整页刷新 GET 返回 200，两处各一条关联；已关联词仍只能查看或清除。浏览器页面和本地隔离服务继续保留，未提交或推送。

## 扩展数据真实验收（2026-09-07）

用户要求增加数据继续测试。新增 11 个任务专用词条、37 个具体词形、17 个词义（包含一个验收草稿），通过真实创建、词形保存、词义保存、发布 API 准备数据。新增已发布候选包括 dress、give、take、care、up、dress up、dress up as、give up、take care of、dress rehearsal；原 mother / my mother 数据继续保留。短语发音使用测试占位值，词义组合用于交互验证，不作为正式词库内容。

新增验收草稿：`association practice`，ID `01a07b25-228e-7bb3-821d-ed30d49d61bf`。页面包含一处通用英文释义、一处英美分栏释义及四条例句；涵盖重复词、连续/不连续词组、Emoji 和换行。

| 验证场景            | 真实页面结果                                                                                                    |
| ------------------- | --------------------------------------------------------------------------------------------------------------- |
| 多词性、多词形      | dress 的名词原形/复数与动词五种词形分开展示；分别选择名词「连衣裙」与动词词义，保存目标词性和词义不同           |
| 相近短语            | dress up 查询同时出现 dress up、dress up as，选择的短语来源正确                                                 |
| 同名成分、多词义    | give up 分别显示「give（放弃）」和「give（交出）」等成分；选择后保留各自 via_phrase.sense_id / component_id     |
| 连续三词短语        | takes care of / took care of 保存完整源范围，目标分别为第三人称单数 takes / 过去式 took                         |
| 不连续短语          | dress … up、gave … up 不包含中间 me / the idea，逗号句号也不属于保存范围                                        |
| 重复词隔离          | 同句三个 Mother / mother 中只关联第一个和第三个，中间位置保持未关联                                             |
| 英美正文隔离        | 英式释义保留 takes care of，美式释义保留 dressed up，未覆盖另一侧                                               |
| Emoji 与跨段        | Emoji 后 Mother 码点范围为 [2,8)；跨段组合被阻止，继续按钮不可用；第二段的 dress … up 可独立建立关联            |
| 多组清除、撤销/重做 | 同句四组关联中清除 dress … up 只删除这一组，其他三组保留；撤销恢复，重做移除，再撤销恢复完整数据                |
| 无成分候选          | dress rehearsal 显示未配置成分用词，节点具有 disabled 样式；实际点击不产生关联                                  |
| 保存与刷新          | 两次 PUT 及整页刷新 GET 均返回 200；13 条关联的稳定 ID、全部目标字段、来源范围、via_phrase 与保存前逐项完全一致 |

最终分布：通用释义 2 条、英式释义 1 条、美式释义 1 条；四条例句分别 4、2、3、0 条，共 13 条且 ID 全部唯一。逐条检查 Unicode 码点范围切回原文均等于 surface。刷新后点击已关联词仍只能查看和清除。

中途连接中断后，验收前后端进程已经退出，但隔离 Postgres / Redis 和全部 14 个词条仍在。恢复了相同配置及既有后端可执行文件：后端 PID 38158（58383）、admin PID 38159（53001）；healthz=ok、readyz=ready、前端 HTTP 200。仍使用原任务隔离容器、真实 API 与关闭业务 mock 的配置。

验收入口：http://127.0.0.1:53001/words/01a07b25-228e-7bb3-821d-ed30d49d61bf/v3/wizard/meanings 。数据清单 `/tmp/tsz-expanded-associations-fixture.json`，生成脚本 `/tmp/tsz-expanded-associations.py`，服务句柄 `/tmp/tsz-expanded-service-handles.json`。用户可以继续在页面验收；未清除测试数据或停止服务。

本轮未发现新的功能问题，未修改产品代码或新增测试代码；复用上一轮已通过的 3053 项全量测试及类型/Lint 检查，不重复运行。未提交、推送或部署。

## 普通输入框与编辑器切换（2026-09-07）

用户确认中文项是误选，中文释义与汉语译文继续普通输入。语法结构、英文释义和英文例句默认收起为普通输入框，右侧增加小编辑图标和「编辑器」。点击后原位展开，完成回到输入框；「完成」位于编辑器内部右下角。只调整前端装配与样式，数据契约和后端不变。

测试覆盖默认未挂载工具栏、按需展开、编辑完成回填、多行文本、原标注和 text_links 迁移、重开后的关联和语速保留、只读/能力开关、音频适配器，以及语法结构原工具能力。旧测试按新入口先点编辑器再操作工具，不增加超时或跳过原用例。

本地扩展数据页初始显示 9 个编辑器入口、0 个展开工具栏，中文项没有编辑器入口。例句 3 的 Emoji 和换行在展开、完成后均保留，原三组关联仍在。已添加的小编辑图标、右下角完成按钮通过截图检查。

用户指出展开编辑器顶部仍错位后，实际测量发现空 badge 行与 flex gap 产生 8px 偏移；仅在此宿主隐藏空行。修正后工具栏外框与相邻 Select 上边缘差值为 0px，完成按钮位于画布边框内部。底部标注层与透明输入层共同保留按钮空间，避免遮住末行文字和停顿标记。

按用户后续要求，「完成」使用 antd primary 主题色按钮，浏览器实测背景为 #0071e3、白色文字。完整 `pnpm test:cov` 通过：185 个文件、3058 个测试，186.93 秒；日志 `/tmp/tsz-compact-editor-coverage.log`。admin 类型与 Lint 检查通过，最终样式调整另由本地截图和位置测量验证。前后端继续在 dev，未提交或推送。

## 每条释义独立一行（2026-09-07）

用户确认将扩展验收页原第 3 条的英、美两份内容拆为第 3、4 条独立释义。现有“添加释义/切换英文方式”本就生成 unified 单输入，本轮仅通过正常 API 调整此任务的测试数据，未增加全库迁移或修改产品代码。

原两份正文分别保存到独立 unified 内容，等级、语法结构绑定、关联、语音及音频设置逐项保留。为符合节点所属位置的约束，新建通用正文变体 ID，第二条使用新的释义 ID。保存后独立 GET 与响应一致，两份各保留一条关联；前两条释义及全部例句（含六条分档译文）与操作前完全相同。

浏览器恢复会话后读取返回 200，截图及 DOM 均确认四条释义每行恰好一个语句输入框。操作前备份 `/tmp/tsz-before-split-definition.json`（0600），验证结果 `/tmp/tsz-split-definition-result.json`。数据和验收页保留；没有代码变化，不重复全量测试。

## 语法结构行对齐（2026-09-07）

语法结构收起为输入框后，原竖排操作按钮把行高撑至 50px，居中的序号比 32px 输入框中心低 9px。仅将该行的拖拽/删除按钮改为横排、操作列调整为 56px，并让序号与操作列按首行 32px 控件对齐。浏览器实测收起时序号、两个输入框及两个操作按钮中心 Y 坐标完全相同；展开一侧编辑器时，其边框、另一侧输入框与操作列顶部保持一致。格式与 diff 检查通过，本轮为静态布局调整，使用实际页面测量和截图验证，未修改或保存用户正在编辑的草稿。

## 编辑器只处理标注与设置（2026-09-07）

用户确认去掉当前 V3 各编辑器的「文本」按钮，改字统一回外部输入框。新增正文只读属性，V3 宿主启用；仅锁定文本输入和变更回调，标注、关联、语速、音色及音频不受影响。Esc 收笔后正文仍只读；文本工具移除后不保留多余的首个分隔线。

定向 3 文件、84 项测试通过：两种编辑器模式禁止改字但仍可添加/撤销停顿，Esc 不解锁；外部改字仍迁移标注和关联，打开/完成往返保留多行文本与设置；语法标注和音频原有回归正常。浏览器分别检查语法结构、英文释义和英文例句，均没有文本按钮，内部 textarea 为 readonly，完成后外部 textarea 不带 readonly。保留用户当前草稿，未发起保存或修改后端。

浏览器还验证了只读正文中的 takes care of 原关联仍可正常打开。完整 `pnpm test:cov` 通过：185 文件、3060 测试，196.85 秒；日志 `/tmp/tsz-no-text-tool-coverage.log`。voice-editor/admin 类型与 Lint、改动文件格式以及 diff 检查通过。未提交、推送或部署。
