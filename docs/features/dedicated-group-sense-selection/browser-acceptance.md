# 浏览器实际验收（2026-09-17）

- 前端：本任务工作区，Vite `http://127.0.0.1:3002`，真实 API 代理至 `http://127.0.0.1:8384/api/v1`。
- 后端：本任务代码构建的独立 binary；PID 与 SHA256 记录在 `/tmp/tsz-dedicated-acceptance-processes.json`。
- 数据：独立 PostgreSQL 容器 `tsz-dedicated-acceptance-pg`，端口 55457，数据库 `tsz_dedicated_acceptance`；Redis 容器 `tsz-dedicated-acceptance-redis`，端口 56457。
- 基础数据由本地 `tsz_latest_20260915` 复制，未复制大型词典释义表。原本地服务、线上服务和原数据库未改动。

## 新建验收样例

| 词条    | ID                                   | 用途                                                         |
| ------- | ------------------------------------ | ------------------------------------------------------------ |
| hang    | 01a0ae15-0d04-7f43-96a3-f43251d2c742 | 动词；通用组 hung 对应全部三义，专用组 hanged 对应两项绑定义 |
| hang up | 01a0ae15-0dbd-70f3-bdb9-bfab1e85f54c | 成分用词、正文关联单词入口                                   |
| sample  | 01a0ae1a-3a02-7ff2-849b-3c54e06251b8 | 英文正文 hang up，用于关联短语入口                           |

样例通过真实服务 API 创建并保存为草稿；选择器的操作通过 Chrome 页面完成，无请求拦截或模拟返回。

## 已实测

1. 短语成分：hang → hung 展示三个词义；切到 hanged 仅展示两项绑定义。
2. 页面选择 hanged 的“绞死；处以绞刑”并保存草稿，后端 PUT 返回 200；只读数据库核对 form/sense ID 后，刷新浏览器仍正确勾选该项。
3. 正文“关联单词”：hang → hung / hanged，同样分别为三义/两义。
4. 正文“关联短语”：选中 hang up 两词 → 短语 hang up → 成分 hang → hung / hanged，同样分别为三义/两义。

页面和进程留给用户继续验收，未清理这些样例。没有在浏览器执行发布（自动测试已覆盖发布校验）。
浏览器验收未新改业务代码；线上存量错误配对盘点仍属于部署准备，尚未执行。

## 词形组卡片交互调整

- 默认通用组不显示状态标签或切换控件；“设为专用组”收进右上角菜单。
- 专用组标题显示“专用”，右侧显示绑定数量或“待绑定词义”，折叠时保留。
- 菜单支持“改为通用组”，保留已有绑定影响提示、保存确认和校验定位。
- Chrome 已实测设置专用、待绑定提示、改回通用；验收样例已还原，未保存临时切换。
- 词形步骤与创建向导 163 项测试通过；admin 类型检查和变更文件 ESLint 通过。

## 选择词义后设为专用组（本轮）

- 浏览器打开第 1 组菜单 → 设为专用组，未选择时确认禁用；只列动词词义，其他组已绑定项禁选。
- 勾选“悬挂；吊着”后确认，卡片显示专用/已绑定 1 个词义；点击保存，真实 forms 请求一次保存组与绑定，刷新正确回显。
- 再改回通用，确认解除绑定及影响后保存；独立数据库核查 scope=general 且第一词义 form_group_id 已清除。
- 最终页面停在第 2 组“编辑专用词义”弹窗，已有两个词义勾选，供用户继续验收。
- 原子绑定及引用保护 HTTP 回归通过；后端库 279、词条 HTTP 95、共享例句 24 项通过。
- 前端全仓本轮一次运行 2753 项通过、2 项既有跳过、1 项未改动的 voice-editor 包加载测试超时；该文件单独复跑 2 项通过，未修改超时或断言。
- 后续能力协商与空释义保留的定向回归 212 项通过（2 项既有跳过），接口契约 395 项通过；admin 类型检查与变更文件 lint 通过。

## 卡片内适用词义（最新版本）

- 已移除标题栏的绑定数量链接及范围菜单；专用组展开后展示编号与释义摘要，右侧“修改”。
- “限定适用词义”与“修改”均在卡片内展开复选区域，不使用 Modal；恢复全部词义位于编辑区。
- Chrome 实际把第 2 组从两义改为三义，确认、保存、刷新后正确回显；随后取消新增绑定，经影响确认保存，已恢复两义样例。
- 最终页面保留第 1 组折叠、第 2 组展开摘要，用户可点击“修改”继续体验。
- 定向验证：词形步骤、词义步骤、创建向导和页面共 306 项通过（2 项既有跳过），提示文案 4 项通过；admin 类型检查、变更文件 ESLint 通过。
- 此轮只调整前端交互与说明，不改后端保存协议，不部署线上。

## 独立交互验收与修正（最新）

- 普通组：未选时确认禁用；其他专用组占用项禁选；勾选后收起/展开仍保留；取消不产生草稿修改。
- 专用组：原绑定正确勾选；清空所有勾选时确认禁用；取消恢复原摘要。
- 多词性隔离：give 名词组只列“弹性；伸缩性”，动词组只列四项动词词义，未保存测试修改。
- 空态：centre 的空形容词组不允许确认；“前往添加词义”进入词义步骤并保持形容词选中。
- 1024×800 窗口：页面宽度与视口同为 1024，无横向溢出，编辑区按钮可见；临时视口已恢复。
- 发现并修正：恢复全部词义再选回原绑定后，JSON 对象键顺序变化造成假的 meanings 脏状态；比较改为复用规范化 JSON 内容，保持数组顺序有意义。
- 新回归先复现 `false/true/true`（词形未改、词义/整体误标未保存），修复后为 `false/false/false`；真正修改释义仍为未保存。
- 修复后的四个相关测试文件共 233 项通过（2 项既有跳过），admin 类型检查、ESLint、Prettier 通过。
- 同一路径已在 Chrome 复验，原绑定恢复后未保存提示消失。样例未额外持久化修改，保存/刷新复用上一轮同一保存链路的真实验收证据。

## 头部“专用词义”入口（最新）

- 按用户要求将“限定适用词义”更名为“专用词义”，统一放在卡片头部右侧、展开/收起按钮之前。
- 普通组与专用组共用入口；正文保留适用词义摘要，移除重复的“修改”和原来的正文入口。
- Chrome 验证：卡片折叠时点击头部入口会自动展开并进入卡片内编辑，已绑定词义正确勾选，无弹窗、无数据修改。
- 167 项词形步骤/向导测试通过；admin 类型检查与变更文件 ESLint 通过。
