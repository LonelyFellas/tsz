# 派生词多选词义

同一个源词义下，同一派生词显示一行，匹配词义允许多选；近义词、反义词保持单选。
三个关联词分类纵向排列，各占整行，词义列获得主要宽度。
无词义草稿及手输词条继续保存纯文本。分类内词条去重、分类间独立选择保持不变。

验收：选两个义项后保存、重新打开和预览均保留两个；取消一个不删除另一个；改选词条、删除行清理整组；发布保留独立目标引用。
不提交、推送或部署；保留本会话此前修改。
