# 实施与验证

> 注：2026-09-10 起档位改为语义化命名 `word_for_word`（初阶）/ `balanced_fluency`（中阶）/ `adapted_creation`（高阶），初/中/高指译文风格而非 CEFR 难度，服务端也不再按例句等级推导档位。本文记录的是当时口径，其中的 `a1_a2` 一组命名与「低阶」叫法均已废弃。

2026-09-07，前后端主工作目录 dev 实施，保留本会话正文关联功能改动。未提交、推送或部署。

## 结果

- 每条例句可添加同档多条译文，每条拥有独立 ID、正文及高/中/低档位，可逐条改档和删除。
- 高阶 a1_a2、中阶 b1_b2、低阶 c1_c2；原“初阶”显示改为“低阶”。例句等级变更不修改译文档位。
- 删除当前 zh_text 兼容别名时保留剩余译文，并更新兼容值；最后一行不能删除，可清空正文保存草稿。
- 数据库仅放开三种分档译文的槽位唯一限制，译文节点身份不再绑定档位。其他文本槽位仍唯一，同档按规范列表顺序存储。
- 既有单条/三档数据、旧客户端省略数组、发布快照继续兼容；请求字段与枚举未增加。每句不再限制三条，沿用词条总节点及请求大小保护。

## 自动检查

| 检查                           | 结果                                                                                               |
| ------------------------------ | -------------------------------------------------------------------------------------------------- |
| 前端完整 pnpm test:cov         | 185 文件、3056 测试通过，生成完整覆盖率报告；186.02 秒                                             |
| api-client 包测试              | 8 文件、419 测试通过；六条响应解码、当前生成契约指纹与 maxItems 验证                               |
| admin / api-client 类型与 Lint | 通过                                                                                               |
| Rust lib                       | 305 通过，1 项原有 ignored                                                                         |
| Rust translation handler       | 4 项通过，包括同档六条保存、修改档位、删除别名、旧客户端省略字段、发布快照、重复 ID 拒绝及词形重存 |
| Rust Clippy / fmt              | --all-targets --all-features -D warnings 与 fmt --check 通过                                       |
| migration 回退保护             | 已存在同档多条时 down 明确拒绝；在隔离库事务内验证并回滚，未提交                                   |

日志使用 `/tmp/tsz-multi-translations-` 前缀：coverage.log、api-tests.log、typecheck.log、lint.log、rust-lib.log、rust-handler.log、clippy.log、fmt.log、rollback-guard.log。

OpenAPI 由后端 export_openapi 生成，再使用显式 OPENAPI_SOURCE 同步前端。原始 spec SHA-256 与 runtime bundle 均为 `8031d28610ed5a8157b6e68bb9c2d59b7a035d627a86783dbeff161f8340ce76`。

## 真实浏览器与数据库

沿用隔离 Postgres `127.0.0.1:55439/text_links_browser`、Redis `127.0.0.1:56389/0`。更新前备份位于 `/tmp/tsz-before-multiple-translations.sql`（仅本任务测试数据，0600）。新 migration 已应用；当前后端为新工作区编译的真实 router，PID 48793、端口 58383，readyz=ready；admin 仍为 PID 38159、端口 53001，业务 mock 关闭。

验收页：`http://127.0.0.1:53001/words/01a07b25-228e-7bb3-821d-ed30d49d61bf/v3/wizard/meanings`。

1. 在例句 1 已有高、中、低三行基础上补齐各两条，保留原行 ID 和正文。页面显示六条独立输入，添加菜单明确标注 A1/A2、B1/B2、C1/C2。
2. 保存 PUT 200，响应包含六条译文，其他三条例句仍各一条；英文人工关联数量仍为 4、2、3、0。
3. 已存在的低阶译文改为高阶后再次 PUT 200，同一 ID、正文保留；再改回低阶。新增第七条并删除，原六条不变。
4. 最终保存后整页刷新 GET 200，整个 meanings 与保存响应完全一致，包含所有译文、英文正文、关联及方言槽位。
5. 完成步骤 PUT 200，进入预览页，六条译文按各自档位逐条展示；低/中/高标记各两条。

发布与不可变历史快照由真实数据库 handler 测试证明；未将这条本地验收草稿发布。服务与测试数据保留供用户继续查看。新的临时后端启动文件已移到 `/tmp/tsz-translations-local-launcher.rs`，不加入产品源码。

配套发布须先应用数据库 migration 和新后端，再发布新前端；旧前端的三条 runtime 限制无法读取扩展数据。不能在已有重复档位或无法还原的退休节点上直接回退 migration，回退脚本会拒绝并保留数据。

## 多条译文排版调整（2026-09-07）

原布局将多条译文堆在右侧，整行高度由译文数量决定，英文下方大量留白；译文删除与整句操作相邻也不易区分。调整为每条例句独立区块，英文在首行，译文在下方按高、中、低分组展示，组内自动换行；单档只显示该档，不预留空分组。

组件测试在既有六条数据增删改场景中验证高/中/低各两条分组、改档后移入对应分组且原 ID 不变；与完整词义页面一起 73 项通过。类型检查与 Lint 通过。

本地当前页面的未保存编辑保留。使用独立临时页面读取已保存的六条译文，在默认 1280px 视口确认三个分组同一水平线、每组两条，长中文自动换行；第一条例句整个区块高度 208px，其他单译文区块为 102px，多行英文为 124px。主页面的大视口也检查了单条译文布局。未保存测试编辑、未更改后端或数据契约。

完整 `pnpm test:cov` 通过：185 文件、3058 测试，209.87 秒；日志 `/tmp/tsz-translation-layout-coverage.log`。临时验收页已关闭，保留用户原页面与未保存草稿；格式及 diff 检查通过。
