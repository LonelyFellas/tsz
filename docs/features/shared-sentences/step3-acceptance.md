# Step 3 关联反查：本地验收

日期：2026-09-13。本次范围为已确认需求的本地实现与验收，没有提交、推送或部署。

## 版本与环境

- 前端：`tsz-main-local-20260912` 工作树，HEAD `068c085` 加未提交修改，已整合 `origin/main@9ae7275` 的等级录入位与词性全称改动。
- 后端：`tsz-rust-main-local-20260912` 工作树，HEAD `221747f`（内容与 `origin/main@d5bddc4` 一致）加未提交修改。
- 页面：`http://localhost:3101`，本任务 Vite 进程，代理 `http://127.0.0.1:8484/api/v1`；WORDS / PART_OF_SPEECH / TTS mock 均为 false。
- 后端已切换到本次构建的 `/tmp/tsz-step3-associations-api`；它的 SHA256 与最终 `cargo build --locked --all-features --bin tsz-rust` 制品相同。进程和制品记录：`/tmp/tsz-voice-entry-services.json`。
- 数据库：本任务容器 `tsz-main-local-20260912-pg`，127.0.0.1:55441/tsz_main_local。页面 Redis 为 56392/0；Rust 自动化使用 SQLx 独立测试库及 56392/15。
- SQLx 原生 prepare 已完成，`.sqlx` 没有变化。本轮没有执行新的结构迁移或清理用户数据。

## 实际浏览器链路

使用 Codex 内置浏览器和已有本地超管会话，未修改账号凭据。

1. 打开 make up 的 Step 3，词义区内没有重复例句清单；一处“多维例句”显示所有指向当前词条的例句。保留原语义区间、词性、词义与 voice-editor。
2. 添加 `We make up a story for this local check.`，初阶和高阶分别填写译文。未关联当前词条前完成按钮不可用；自动检测给出 `make up` 的具体位置，点击确认后状态变为已关联当前词条。
3. 打开原 voice-editor，通过“关联单词”选择正文中的 story。候选只显示实际匹配的 story，确认后点击完成，独立保存成功；词义保存按钮仍未触发。
4. make up 列表出现新例句，完整显示填写的初阶、高阶译文。独立数据库查询确认仅保存两条非空译文、两个 Linked 目标，没有新增 collection。
5. 新开 story 页面，在没有词性/词义内容的情况下也能直接查询到同一句；不需要再次收录。进入编辑器提示影响两个关联词条。
6. 从 story 编辑初阶译文并完成；刷新 make up 页面读到同一份修改。
7. 从 story 点击“从当前词条解除关联”并确认，story 列表从 5 条变为 4 条，新样例消失；make up 仍保留此句及两条译文。
8. 最后复核窄窗口：正文与译文正常换行，操作按钮没有挤压正文。例句区块放在 Step 3 内容区内、底部词义保存按钮之前。内置浏览器保留 make up 验收页。

最终数据库复核：样例 revision 为 3，未删除，保留 2 条译文，仅关联 make up，collection 数量为 0；make up 词条 revision 仍为 7。

保留的本次样例：例句 UUID `b6884a16-ef41-4117-a17b-20696a19d0e9`。来源/仍关联词条 make up：`01a09551-1747-7e81-9d8a-c751db933cf5`；已解除 story：`01a09551-3b0c-7851-a1ce-d4f96a375f90`。这是本轮专用验收数据，供用户继续检查，不改写之前的例句。

## 自动化与检查

- 后端新模型定向集成：15 项通过，覆盖反查、去重、精确词条、完整 token、登记变形/非连续短语、Pending、独立保存、幂等重放、解除、归档旧关系修复、权限与并发保护。
- 后端完整质量门：fmt、clippy（all targets/all features，warnings denied）、all-features 测试通过；完整测试累计 886 项通过；之后的读取权限小修复额外通过最终 15 项模块回归及独立审查。最终 fmt/clippy/构建均已复核通过，最终制品已重新启动在 8484。
- API 契约：391 项通过。OpenAPI 从明确的本地后端导出并同步，新增 targets root 已进入严格 runtime bundle，旧 collected/sense 与 collections 契约退出。
- 页面功能回归：例句与向导定向通过；Step 3 / 向导 / 页面三个文件 216 项通过、2 项原有跳过；新增离开保护测试所在向导文件 84 项通过。
- 前端全仓 typecheck、lint 已通过；页面 build 已通过。实看后的排版小改动已通过受影响回归。
- 全量前端覆盖率检查 `pnpm test:cov --maxWorkers=1` 通过：175 个文件、2597 项通过，2 项原有跳过。最初两轮有页面用例触发 5 秒超时；已将大表单的按钮查找改为精确文本/aria-label，保留原断言与超时。该页面单文件带覆盖率复验 42 项通过、2 项原有跳过，之后全量复验通过。

验证日志位于 `/tmp/tsz-step3-implementation-20260913/`；此前的 `local-acceptance.md` 属于旧模型，不能替代本记录。

## 后续交互微调：直接展示编辑器

新增与编辑例句直接展示可输入正文的 voice-editor，保留“文本 / 关联单词 / 关联短语”切换；无需再点话筒。通过 V3VoiceTextField 的 editor 展示方式仅对例句入口启用，其他词义/发音字段保留原输入框入口。

本轮覆盖新增空白正文输入、编辑正文与译文后保存、句内关联及原字段行为，28 项定向回归、admin typecheck 与 lint 均通过。内置浏览器已确认当前编辑页直接显示工具栏和正文；保留用户正在操作的关联选择，不刷新或提交其输入。

## 后续修复：改错再改回自动恢复关联

复现：连续 `make up` 与非连续 `make … up` 改成 `makke` 再改回后，原实现仍禁用完成；两项回归先失败后通过。

修复在 voice-editor 中为例句入口启用临时关联恢复，纳入原有撤销/重做快照；完整英文还原后恢复原 ID 与位置。手动清除/重新选择关联及加载外部内容会重新确定恢复边界，不会移植到其他重复词。移除了例句宿主中恢复后仍残留的失效警告。

定向验证：voice-editor 全包 240 项、admin 相关 30 项通过，typecheck 与 lint 通过。内置浏览器在临时页面打开已有验收例句，输入 makke 后按钮禁用，改回 make 后直接恢复“已关联当前词条：make up”且按钮可用；没有提交本次试输内容。完整前端覆盖率检查 `pnpm test:cov` 已通过，修复期间未更改后端或数据库。

## 后续修复：连读弧线按实际字母贴合

用户要求保留贴字与自然倾斜。旧规则按整段锚点选区的最高墨迹定位，多字母选区中心落在矮字母上时，端点会被旁边的高字母抬起。新增回归用例先复现这一偏差，再验证逐字素落点。

编辑器与只读展示现在共用文本 Range 的逐字素测量，取靠近选区中心的实际字母及其字体、字重；不计音标后缀和占位伪元素。两端各自保持距墨迹顶部 0.06em，控制点各自抬升，字母高低不同时自然倾斜。字重变化也触发重新测量。没有修改连读标注的数据或关联关系。

同一句、同一 Ubuntu 700 / 26px 字体的浏览器对照中，三条弧的左/右端点高度差从 6.14、−2.47、0 px 变为 6.14、3.75、6.14 px：左侧落点保持，中间与右侧贴回中心字母。对照页面与数据存于 `/tmp/tsz-liaison-ink-contact/`，临时页面已关闭并从应用目录移除，用户原编辑页未刷新或提交。

定向验证：voice-editor 243 项及 admin 相关 30 项通过，类型检查与 lint 通过；首轮高并发出现两个原有用例超时，限制为两个 worker 后整包通过，未调整断言或超时。全量覆盖率 `pnpm test:cov` 已通过。

## 数据与发布边界

切换前只读清点得到 18 条有效例句、60 条 Linked 标注、1 处 collection-only 关系和 2 处旧 sense collection。旧数据全部保留；collection-only 不再决定词条列表，需要在全局例句库补齐真实关联。此后仅新增并操作上面列出的验收样例。

当前接口与已部署旧客户端不兼容：新客户端拒绝旧响应额外字段，旧客户端缺少新响应字段且 collections 路由已退出。将来发布需配套维护窗口与新的数据清点；本轮不执行发布，不沿用此前清理 29 条测试数据的授权。

可重复的后端验收命令（先准备隔离测试数据库与 Redis）：

```sh
SQLX_OFFLINE=true cargo test --locked --test shared_sentences
```
