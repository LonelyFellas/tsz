import { Card } from "@tsz/ui";

// 班级管理:添加词表 → 系统生成班级长期任务。
export function ClassManager() {
  return (
    <section>
      <h1 className="mb-4 text-xl font-bold">班级管理</h1>
      <Card>班级列表、成员、添加词表生成长期任务在此实现。</Card>
    </section>
  );
}
