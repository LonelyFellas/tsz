// 审核中心:敏感词审核、公开词表审核、老师申请审核(通过/拒绝并填原因)。
import { Typography } from "antd";

export function ReviewsPage() {
  return (
    <Typography.Title level={4} style={{ margin: 0 }}>
      审核中心
    </Typography.Title>
  );
}
